import React from 'react';

import "../../App.scss";

export const UnauthorizedUserProfile = () => {
    return (
        <div className="user-profile-message">
            Sorry, but you are non-authorized
        </div>
    );
};